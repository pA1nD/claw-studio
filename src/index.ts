/**
 * Claw Studio — entry point
 * The loop is implemented in v0.1.
 */
export const VERSION = "0.0.1";

/** Returns a greeting. Used to verify the build works. */
export function greet(): string {
  return `Claw Studio v${VERSION} — Claw your way.`;
}
